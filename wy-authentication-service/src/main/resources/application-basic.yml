server:
  port: 8002
spring:
  application:
    name: wy-authentication
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    schema:
      - classpath:schema-authentication.sql
logging:
  level:
    com.wenyu7980.authentication.common: debug